# from django.db import models

# class ImageUpload(models.Model):
#     image = models.ImageField(upload_to='images/')
    
#     resized_image = models.ImageField(upload_to='images/resized/', blank=True, null=True)
    
#     def __str__(self):
#         return f"Image {self.id}"
